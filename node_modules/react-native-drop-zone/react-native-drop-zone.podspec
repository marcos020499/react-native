require 'json'

package = JSON.parse(File.read(File.join(__dir__, 'package.json')))

Pod::Spec.new do |s|
  s.name             = "react-native-drop-zone"
  s.version          = package['version']
  s.summary          = package['description']
  s.homepage         = "https://github.com/WeTransfer/react-native-drop-zone"
  s.license          = package['license']
  s.author           = package['author']
  s.platform         = :ios, "11.0"
  s.source           = { :git => "https://github.com/WeTransfer/react-native-drop-zone.git", :tag => "v#{s.version}"  }
  s.source_files     = "src/ios/**/*.{h,m,swift}"
  s.swift_version    = "5.0"

  s.dependency "React"

end

  
